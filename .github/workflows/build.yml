name: Build

on:
  workflow_dispatch:
  push:
    branches:
      - "master" 

env:
  DOTNET_VERSION: "9.x"

jobs:
  build:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: AutomatedDirectoryCleanup

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Workload Restore
        run: dotnet workload restore

      - name: Restore
        run: dotnet restore AutomatedDirectoryCleanup.sln

      # TODO: Action for checking vulnerable/out-of-date packages

      - name: Build
        run: dotnet build AutomatedDirectoryCleanup.sln --configuration Release --no-restore

      - name: Test
        run: dotnet test AutomatedDirectoryCleanup.sln --configuration Release --no-restore --no-build

      #- name: Publish
      #  run: dotnet publish AutomatedDirectoryCleanup.sln --configuration Release --no-restore --no-build